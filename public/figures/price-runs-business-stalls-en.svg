<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 960 540" width="960" height="540" role="img" aria-label="The price line keeps rising while the business line flattens at the midpoint, and the two drift further apart after the dashed line.">
  <style>
    svg { --page:#ffffff; --card:#f6f8fc; --line:#dde3ee; --ink:#1a212b;
          --dim:#57616f; --grey:#b9c2d0; --accent:#1f6feb; --warn:#d97706; }
    @media (prefers-color-scheme: dark) {
      svg { --page:#0d1117; --card:#161b22; --line:#272e39; --ink:#e6edf3;
            --dim:#8b949e; --grey:#4a5464; --accent:#58a6ff; --warn:#d29922; }
    }
    text { font-family:"Noto Sans TC","Microsoft JhengHei","PingFang TC",sans-serif; }
    .t { font-size:27px; fill:var(--ink); font-weight:700; }
    .g { font-size:20px; fill:var(--ink); font-weight:600; }
    .b { font-size:16px; fill:var(--dim); }
    .c { font-size:22px; fill:var(--ink); }
  </style>
  <rect x="0" y="0" width="960" height="540" fill="var(--page)"/>
  <text class="t" x="60" y="58">Price is moving; is the business keeping up</text>

  <line x1="100" y1="440" x2="900" y2="440" stroke="var(--line)" stroke-width="2"/>
  <line x1="400" y1="140" x2="400" y2="440" stroke="var(--warn)" stroke-width="2" stroke-dasharray="6 6"/>
  <text class="g" x="352" y="128">Split point</text>

  <polyline points="100,420 250,368 400,300 550,222 700,178 880,196"
            fill="none" stroke="var(--accent)" stroke-width="4"/>
  <polyline points="100,420 250,384 400,356 550,350 700,354 880,358"
            fill="none" stroke="var(--dim)" stroke-width="4"/>

  <text class="g" x="700" y="156">Price</text>
  <text class="b" x="560" y="392">Customers · renewals · spend per customer</text>

  <text class="c" x="100" y="492">Where the lines part is where you ask again</text>
</svg>